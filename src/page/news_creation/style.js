
import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles(theme => ({
    newsCreationContent_textfield: {
        marginTop: theme.spacing(2),
        marginBottom: theme.spacing(2)
    },

    articleCreation_button: {
        display: 'block',
        marginTop: theme.spacing(2)
    }

}));
