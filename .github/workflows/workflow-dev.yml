name: Dev workflow

on:
  workflow_dispatch:

env:
  API_URL: 'test'

jobs:
  workflow_v1:
    steps:
      - uses: M4rk98/feed.react/.github/workflows/code-quality.yml@master
        with:
          environment: 'dev'
      - name: 'Echo'
        run: echo "The operating system on the runner is $env:RUNNER_OS. $env:API_URL"
